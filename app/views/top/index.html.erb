<!DOCTYPE html>
<html>
  <head>
    <title>Framelate</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
  </head>

  <body>
    <div class="bg-white text-black p-4">
      <% if current_user %>
        <p>ログイン中のユーザー: <%= current_user.email %></p>
      <% else %>
        <p>ログインしていません。</p>
      <% end %>
      <% if @templates.present? %>
        <div class="mb-3">投稿テンプレート一覧↓</div>
        <%= render partial: 'template', collection: @templates %>
      <% else %>
        <div class="mb-3">テンプレートがありません。</div>
      <% end %>
    </div>
  </body>
</html>
